{
  "name": "notespacer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "build-client": "cd client && npm run build",
    "build-storybook": "cd client && npm run build-storybook",
    "build": "npm run build-storybook && npm run build-client",
    "client": "cd client && npm start",
    "storybook": "cd client && npm run storybook",
    "test": "cd client && npm test",
    "postinstall": "cd client && npm install",
    "server": "json-server --watch db.json --port 8080 --routes routes.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/golubs/notespacer.git"
  },
  "author": "Oleg Golubs",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/golubs/notespacer/issues"
  },
  "homepage": "https://github.com/golubs/notespacer#readme",
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-plugin-react": "^7.17.0",
    "json-server": "^0.15.1",
    "prettier": "1.19.1"
  }
}
