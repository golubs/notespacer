import styled from '@emotion/styled';

const TextAllCapsPrimary = styled.b`
  font-family: Arial;
  font-size: 14px;
  text-align: center;
  color: ${props => props.theme.colors.primary};
`;

export default TextAllCapsPrimary;
